#include <iostream>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int n;
        cin >> n;
        n == 0 ? (cout << "YES" << endl) : (cout << "SURE" << endl);
    }
}
