/* INCOMPLETE */
#include <cstdio>
#include <cstring>
using namespace std;

char s[1000];

int main()
{
    while (scanf("%s", s) == 1)
    {
        if (strlen(s) % 2) {
            printf("NO\n");
            continue;
        } 
        bool flg;
        int length = strlen(s);
        int half = length / 2;
        
        strchr(s[0]+1)

        if (flg) printf("YES\n");
        else printf("NO\n");
    }
}
